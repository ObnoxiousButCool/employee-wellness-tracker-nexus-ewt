PORT=4000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ewt?schema=public
# Must match the frontend's JWT_SECRET exactly: it verifies the ewt_token
# cookie's signature locally rather than trusting an unsigned mirror cookie.
JWT_SECRET=replace-with-a-long-random-secret
