BACKEND_URL=http://localhost:4000
# Must match the backend's JWT_SECRET exactly: the frontend verifies the
# ewt_token cookie's signature locally (see lib/session.js) rather than
# trusting an unsigned mirror cookie for authorization decisions.
JWT_SECRET=replace-with-a-long-random-secret
